{test5: program to test real variables.}
{Already tested: assignment, write statements, integer variables, and integer and real constants.}
program test5;
var x, arealnumb real;
    y real;
    int integer;
begin
  x := 100.23;
  write(X);
  writeln;
  arealnumb := x;
  write(ARealNumb);
  writeln;
  Y := 765;
  write(y);
  writeln;
  AREALNUMB := Y;
  write(arealnumb);
  writeln;
  int := x;
  write(INT);
  writeln;
  arealnumb := INT;
  write(arealnumb);
  writeln;
end.
